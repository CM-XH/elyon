
/**
 * Supabase client removed.
 * Persistent data is now managed via LocalStorage in App.tsx and AdminDashboard.tsx.
 */
export const saveToSupabase = async (data: any) => { return false; };
export const loadFromSupabase = async () => { return null; };
